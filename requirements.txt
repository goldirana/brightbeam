# Core
pydantic>=2.0
pydantic-settings>=2.0
click>=8.0
httpx>=0.25.0
numpy>=1.24.0
pyyaml>=6.0
python-dotenv>=1.0.0

# Feature Extraction (Traditional ML)
scikit-learn>=1.3.0
vaderSentiment>=3.3.2

# Enhanced NER (install with: pip install spacy && python -m spacy download en_core_web_sm)
spacy>=3.6.0

# Dashboard
# Plotly.js is loaded via CDN in HTML - no Python dependency needed

# Dev
pytest>=7.0
pytest-asyncio>=0.21.0
